<form method="post" action="{{ path('lms_delete', {'id': l_m.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ l_m.id) }}">
    <button class="btr">Effacer</button>
</form>
