{% extends 'base.html.twig' %}

{% block title %}Monsters{% endblock %}

{% block body %}
    <h1>Monsters</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ monster.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ monster.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ monster.description }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ monster.slug }}</td>
            </tr>
            <tr>
                <th>Weight</th>
                <td>{{ monster.weight }}</td>
            </tr>
            <tr>
                <th>Height</th>
                <td>{{ monster.height }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_monsters_index') }}">back to list</a>

    <a href="{{ path('admin_monsters_edit', {'id': monster.id}) }}">edit</a>

    {{ include('admin/monsters/_delete_form.html.twig') }}
{% endblock %}
